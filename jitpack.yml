jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.10-tem
  - sdk use java 17.0.10-tem

install:
  - FORCE_JAR_INSTALL=true ./gradlew :tessera:publishToMavenLocal :tessera-hilt:publishToMavenLocal
